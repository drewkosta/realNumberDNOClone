services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: realnumber
      POSTGRES_PASSWORD: realnumber
      POSTGRES_DB: realnumber
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
